[package]
name = "netusage-common"
version = "0.1.0"
edition = "2021"
description = "Tipos compartidos y comprobaciones de entorno (preflight) de netusage"
license = "MIT"

[dependencies]
rustix = { version = "1", features = ["fs", "system", "process"] }
caps = "0.5"
