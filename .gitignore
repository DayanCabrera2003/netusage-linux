# Artefactos de compilacion de Cargo (workspace y crate eBPF, que compila aparte).
target/

# Copias de seguridad de rustfmt.
**/*.rs.bk

# Cargo.lock se versiona: el workspace produce binarios y queremos builds reproducibles.
